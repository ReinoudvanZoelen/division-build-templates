import { IEnvironment } from "./environment.interface";

export const environment: IEnvironment = {
    firebase: {
        projectId: "<-- Add the projectId here -->",
        appId: "<-- Add the appId here -->",
        databaseURL: "<-- Add the databaseURL here -->",
        storageBucket: "<-- Add the storageBucket here -->",
        apiKey: "<-- Add the apiKey here -->",
        authDomain: "<-- Add the authDomain here -->",
        messagingSenderId: "<-- Add the messagingSenderId here -->",
    }
};
